import styled from 'styled-components';

const GlobalStyled = {
    ViewCol: styled.div`
        flex: 1;
        display: flex;
        flex-direction: column;
    `,
    ViewRow: styled.div`
       flex: 1;
       display: flex;
       flex-direction: row;
   `,
}

export default GlobalStyled;